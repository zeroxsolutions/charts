apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "kong.fullname" . }}-test-connection"
  labels:
    {{- include "kong.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "kong.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
