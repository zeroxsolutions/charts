# Copyright ZeroX.

# Global
global:
  kubeVersion:

  nameOverride:

  fullnameOverride:

  namespaceOverride:

  commonLabels: {}

  commonAnnotations: {}

  clusterDomain: cluster.local

# Image
image:
  registry:

  repository: linuxserver/code-server

  tag:

name: code-server

selectorLabels: {}

containerPorts:
  http:

emptyPassword: false

password:

hashedPassword:

sudoPassword:

sudoPasswordHash:

proxyDomain:

defaultWorkspace:

extraArgs:

extraEnvVars:

service:
  create:

  type:

  ports:
    http:

  clusterIP:

  nodePort:

serviceAccount:
  create:

  name:

  annotations: {}

ingress:
  enabled:

  annotations:

  ingressClassName:

  pathType:

  extraRules:

  extraPaths:

  host:

  extraHosts:

  tls:

  extraTls:
    # - key:
    #   crt:

persistence:
  create: false

  storageClassName:
