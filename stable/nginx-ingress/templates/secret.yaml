{{- if .Values.secret.create }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "nginx-ingress.fullname" . }}
  namespace: {{ .Release.Namespace }}
type: {{ default "kubernetes.io/tls" | .Values.secret.type }}
data:
    {{- if .Values.secret.tls }}
    {{- with .Values.secret.tls }}
    tls.crt: {{ .crt | b64enc }}
    tls.key: {{ .key | b64enc }}
    {{- end }}
    {{- end }}
{{- end }}
