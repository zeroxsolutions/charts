apiVersion: v1
kind: Service
metadata:
  name: {{ include "grafana-k6.fullname" . }}
  labels:
    {{- include "grafana-k6.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "grafana-k6.selectorLabels" . | nindent 4 }}
