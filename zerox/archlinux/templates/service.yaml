apiVersion: v1
kind: Service
metadata:
  name: {{ include "archlinux.fullname" . }}
  labels:
    {{- include "archlinux.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "archlinux.selectorLabels" . | nindent 4 }}
